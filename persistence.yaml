services:
  postgres:
    volumes:
      - "${PWD}/.tmp/data/postgres:/var/lib/postgresql/data"

  minio:
    volumes:
      - "${PWD}/.tmp/data/minio:/data"

  meilisearch:
    volumes:
      - '${PWD}/.tmp/data/meili_data:/meili_data'
